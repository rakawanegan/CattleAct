# データ拡張のデフォルト設定
use: true

randaugment:
  num_ops: 3
  magnitude: 7

masking_from_skeleton:
  use: true
  cutout_prob: 0.5
  n_holes: 3
  scale: [0.02, 0.1]
  ratio: [0.5, 3.0]
  skip_label: false
  unuse_low_conf_skel: true